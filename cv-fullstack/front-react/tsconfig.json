{
  "compilerOptions": {
    "target": "ES6", // Define a versão do ECMAScript para ES6
    "module": "ESNext", // Usa módulos ESNext
    "lib": [
      "DOM",
      "DOM.Iterable",
      "ESNext"
    ], // Bibliotecas padrão incluídas
    "baseUrl": "./src", // Define o diretório base para importações
    "paths": {
      "@/*": [
        "./*"
      ] // Permite importações absolutas a partir de `src/`
    },
    "strict": true, // Habilita todas as verificações de tipo estritas
    "esModuleInterop": true, // Permite a interoperabilidade entre CommonJS e ES Modules
    "skipLibCheck": true, // Ignora a verificação de tipos nas bibliotecas instaladas
    "forceConsistentCasingInFileNames": true, // Força consistência no uso de maiúsculas/minúsculas em nomes de arquivos
    "moduleResolution": "node", // Usa a resolução de módulos do Node.js
    "resolveJsonModule": true, // Permite a importação de arquivos JSON
    "isolatedModules": true, // Garante que cada arquivo seja um módulo independente
    "noEmit": true, // Não gera arquivos de saída (apenas verificação de tipos)
    "jsx": "react-jsx" // Habilita suporte a JSX com React
  },
  "include": [
    "src"
  ], // Inclui todos os arquivos na pasta `src`
  "exclude": [
    "node_modules"
  ], // Exclui a pasta `node_modules`
  "allowImportingTsExtensions": true
}